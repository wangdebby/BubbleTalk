<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/themee">


  <Button
      android:id="@+id/btn_back_theme"
      android:layout_width="155dp"
      android:layout_height="71dp"
      android:layout_alignParentStart="true"
      android:layout_alignParentTop="true"
      android:layout_marginStart="2dp"
      android:layout_marginLeft="16dp"
      android:layout_marginTop="31dp"
      android:layout_marginEnd="16dp"
      android:layout_marginRight="16dp"
      android:layout_marginBottom="16dp"
      android:background="@android:color/transparent"
      android:text="返回            ㄈㄢˇㄏㄨㄟˊ"
      android:textColor="#000000"
      android:textSize="18sp" />

  <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/recyclerViewThemes"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_below="@id/btn_back_theme"
      android:padding="12dp" />

  <TextView
      android:id="@+id/textView"
      android:layout_width="133dp"
      android:layout_height="wrap_content"
      android:layout_above="@+id/recyclerViewThemes"
      android:layout_alignParentEnd="true"
      android:layout_marginEnd="117dp"
      android:layout_marginBottom="30dp"
      android:text="主題Theme"
      android:textSize="24sp" />

</RelativeLayout>
